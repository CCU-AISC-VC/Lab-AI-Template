請先閱讀專案根目錄的 `instructions.md`，並在本 repo 的所有工作中完整遵守其中的規則。

重點摘要（完整規則以 `instructions.md` 為準）：

- 不得修改、刪除、覆寫 `data/raw/` 內的任何檔案
- 不得自行產生假資料，找不到資料就停下來問
- 規劃階段不得寫程式碼，只產出 `plans/` 底下的 PLAN 檔案
- 一次不要修改超過三個檔案
- 單位、座標系、資料格式一律以 `SPEC.md` 為準，不得自行假設
