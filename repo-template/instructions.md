# Agent 工作規則

> 這份是本 repo 的常駐規則。任何 AI agent 開始工作前必須完整讀過。
> 使用方式：在你的工具設定檔中指向這份檔案，或在對話開頭整份貼上。

---

## 一、專案基本資訊

| 項目 | 內容 |
|---|---|
| 專案名稱 | （填寫） |
| 負責人 | （填寫） |
| 目標 | （一到兩句話說明這個 repo 要解決什麼問題） |
| 目前進度 | （填寫，每次階段性完成後更新） |

## 二、技術環境

- 語言與版本：Python 3.11（或 MATLAB R2024a 等，請填實際使用的）
- 主要套件：numpy, scipy, pandas, matplotlib
- 執行方式：`python scripts/<檔名>.py`
- 不要引入未列在上表的套件，需要時先問我

## 三、資料

- 原始資料位置：`data/raw/`
- 處理後資料位置：`data/processed/`
- 資料格式、單位、座標系定義一律以 `SPEC.md` 為準，**不要自行假設**

## 四、禁止事項

以下是硬性規定，違反即停止並回報：

1. **不得修改、刪除、覆寫 `data/raw/` 內的任何檔案。** 這是原始量測資料，不可逆。
2. **不得自行產生假資料。** 找不到資料檔、欄位不存在、格式不符，一律停下來問我，不要用隨機數或範例資料代替。
3. **不得在規劃階段寫程式碼。** 規劃階段只產出 `plans/` 底下的 PLAN 檔案。
4. **一次不要修改超過三個檔案。** 需要更大範圍的改動，先說明再動手。
5. **不確定就問，不要猜。** 尤其是單位、座標系、取樣率、感測器設定。

## 五、工作流程

所有任務一律照三階段進行，每一階段結束後停下來等我確認：

### 階段 1：規劃
- 在 `plans/` 建立 `YYYY-MM-DD_<任務名>.md`，格式參考 `plans/PLAN_template.md`
- 開始規劃前，先問我三個你認為最關鍵、但目前資訊不足的問題
- 這個階段**不要寫任何程式碼**

### 階段 2：執行
- 我確認 PLAN 之後才開始
- 一次只做 PLAN 中的一個步驟，做完回報結果，等我說繼續
- 腳本放 `scripts/`，命名 `YYYY-MM-DD_<用途>.py`
- 可重複使用的函式抽到 `src/`

### 階段 3：驗證與產出
- 依下方「物理合理性檢查」逐條檢查，並在回報中列出檢查結果
- 產出三件事，缺一不可：
  1. 圖或表，存到 `results/figures/` 或 `results/tables/`
  2. 一段文字說明結果代表什麼
  3. 可以重跑的腳本
- 完成後在 `DEVLOG.md` 最後面append一筆紀錄

## 六、物理合理性檢查

每次產出數值結果，必須自行檢查並回報以下項目：

- [ ] **單位**：所有物理量的單位是否一致？有沒有 mm/m、g/(m/s²)、Hz/rad·s⁻¹ 混用？
- [ ] **數量級**：結果的數量級是否合理？（例如鋼材應力應在 10⁶–10⁸ Pa 等級）
- [ ] **守恆量**：能量、質量、動量在計算前後是否守恆？誤差多少？
- [ ] **邊界與初始條件**：是否與 `SPEC.md` 中的設定一致？
- [ ] **極端情況**：把阻尼設為 0、外力設為 0、剛度設為極大值，結果是否符合物理直覺？
- [ ] **已知解對照**：若有解析解或教科書標準案例可比對，請比對並報告誤差

若任何一項無法通過或無法檢查，明確說出來，不要略過。

## 七、輸出與命名規則

- 圖檔：`results/figures/YYYY-MM-DD_<內容>.png`，300 dpi，座標軸必須標註物理量與單位
- 表格：`results/tables/YYYY-MM-DD_<內容>.csv`
- 中間資料：`data/processed/`，檔名須能看出來源與處理方式

## 八、回報格式

每次回報請包含：

1. 這次做了什麼（對應 PLAN 的哪一步）
2. 結果是什麼
3. 物理合理性檢查結果
4. 有什麼不確定或需要我決定的地方
5. 下一步建議

---

## 修訂紀錄

| 日期 | 修改內容 |
|---|---|
| YYYY-MM-DD | 初版 |
