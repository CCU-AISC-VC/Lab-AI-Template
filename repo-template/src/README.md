# src/ — 可重複使用的函式

放**被多個腳本共用**的東西：讀檔、單位轉換、濾波、繪圖樣式。

判準：同一段邏輯第二次要用到，就從 `scripts/` 抽到這裡。

- 檔名用功能命名，不加日期：`io.py`、`units.py`、`filters.py`
- 只放函式與類別，不要有執行時的副作用
- 單位轉換係數一律引用 `SPEC.md` 的值，並在 docstring 註明出處
