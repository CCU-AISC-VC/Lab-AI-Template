# 規格書 SPEC

> 這份定義所有「agent 看不出來、但錯了會全盤皆錯」的資訊。
> 實驗設定或資料格式一改變，立刻更新這份。

---

## 一、單位系統

本專案一律使用以下單位，程式碼中不得出現其他單位：

| 物理量 | 單位 | 備註 |
|---|---|---|
| 長度 | m | 圖表可換算為 mm 顯示，但需標註 |
| 質量 | kg | |
| 時間 | s | |
| 力 | N | |
| 應力 | Pa | |
| 加速度 | m/s² | 感測器原始輸出為 g，需乘 9.80665 |
| 角度 | rad | 圖表顯示可用 deg，需標註 |
| 頻率 | Hz | 角頻率 rad/s 請明確標示為 omega |

## 二、座標系定義

- 原點：（描述位置，例如：試件左下角固定端）
- X 軸：（方向，例如：沿樑軸向，指向自由端為正）
- Y 軸：（方向）
- Z 軸：（方向，右手定則）
- 重力方向：（例如：−Z）

（建議附一張示意圖，放在 `results/figures/coordinate_system.png`）

## 三、感測器與量測設定

| 感測器 | 型號 | 量測物理量 | 量程 | 取樣率 | 安裝位置 | 原始單位 |
|---|---|---|---|---|---|---|
| ACC-1 | | 加速度 | ±50 g | 5000 Hz | | g |
| LC-1 | | 力 | 0–5 kN | 1000 Hz | | mV → N，校正係數見下 |

**校正係數**
- LC-1：`F[N] = V[mV] * <係數>`，校正日期 YYYY-MM-DD

## 四、資料檔格式

### `data/raw/` 檔名規則
```
YYYYMMDD_<試件編號>_<工況>_<重複次數>.csv
例：20260902_S03_load2kN_r01.csv
```

### CSV 欄位定義

| 欄名 | 意義 | 單位 | 備註 |
|---|---|---|---|
| `time` | 時間 | s | 從觸發時刻起算，非絕對時間 |
| `acc_x` | X 向加速度 | g | 需轉換為 m/s² |
| `force` | 荷重 | mV | 需套用校正係數 |

- 檔頭：前 N 行為儀器輸出的 metadata，讀檔時需跳過
- 缺值表示方式：（例如空字串 / NaN / -9999）
- 已知問題：（例如「S02 試件第 3 次量測 acc_y 通道故障，資料無效」）

## 五、分析參數的預設值

| 參數 | 預設值 | 說明 |
|---|---|---|
| 濾波器 | Butterworth 4 階低通 | |
| 截止頻率 | 500 Hz | |
| FFT 窗函數 | Hanning | |
| 去趨勢 | 減去平均值 | |

## 六、已知的物理量參考值

供合理性檢查用：

| 項目 | 預期值/範圍 | 來源 |
|---|---|---|
| 一階固有頻率 | 約 xx Hz | 理論計算 / 文獻 |
| 楊氏模數 | 200 GPa | 材料規格書 |

---

## 修訂紀錄

| 日期 | 修改內容 |
|---|---|
| YYYY-MM-DD | 初版 |
